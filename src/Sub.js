import React from 'react';

const Sub = () => {

    // rebuild 수행의 분기점을 지정할 수 있는 함수가 존재
    // 해당 컴포넌트의 상태값 변경이 없다면, 리빌드를 막을 수 있다. 
    return (
        <div>
            <h1>Sub 입니다.</h1>
        </div>
    );
};

export default Sub;